export enum IPCEvent {
  Search = "search",
  Results = "results",
  Execute = "execute",
  SelectNextResult = "select-next-result",
  SelectPreviousResult = "select-previous-result",
  ClearResults = "clear-results",
}
